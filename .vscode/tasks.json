{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sample",
            "type": "shell",
            "command": "gcc",
            "args": ["LLauncher.c", "-o", "LLauncher", "-mwindows"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}